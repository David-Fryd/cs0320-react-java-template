# Ignore the stuff generated by mvn package, do it yourself after install!
backend/target/

# node_modules can be installed using yarn install, shouldn't be added to repo
node_modules

# Mac metadata file
.DS_Store